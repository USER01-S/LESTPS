#pragma once

constexpr int TILE_SIZE = 64;    // taille d'une case en pixels
constexpr int WINDOW_W  = 512;   // 8 colonnes × 64
constexpr int WINDOW_H  = 512;   // 7 lignes × 64 (carte) + 64 (HUD)
constexpr int JOUEUR_W  = 32;
constexpr int JOUEUR_H  = 32;
constexpr int ENNEMI_W  = 32;
constexpr int ENNEMI_H  = 32;
